---
/** Props for an empty-state panel that can render text or slotted content. */
interface Props {
    /** Optional message shown instead of the default slot. */
    text?: string;
}

const { text } = Astro.props;
---

<div
    data-testid="empty-state"
    class="text-center py-12 bg-slate-800/50 backdrop-blur-sm rounded-xl border border-slate-700"
>
    {text ? (
        <p data-testid="empty-state-text" class="text-slate-300">{text}</p>
    ) : (
        <slot />
    )}
</div>
